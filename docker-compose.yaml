version: '3'

services:
  db:
    image: postgres:17.6
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    container_name: TesloDb
    volumes:
      - ./postgres:/var/lib/postgresql/data
